<?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.dao.CaiWuDao">

    <insert id="insert">
          INSERT INTO finance ( number, salename, huohao, quantity, money, time, year, month )
          VALUES
	      (#{number,jdbcType=VARCHAR},#{saleName,jdbcType=VARCHAR},#{huohao,jdbcType=VARCHAR},#{quantity,jdbcType=INTEGER},#{money,jdbcType=DOUBLE},#{time,jdbcType=DATE},#{year,jdbcType=VARCHAR},#{month,jdbcType=VARCHAR})
    </insert>

    <delete id="deleteFinance">
         DELETE FROM finance WHERE number=#{number,jdbcType=VARCHAR}
    </delete>

    <update id="updateFinance">
         UPDATE finance
         SET salename=#{saleName,jdbcType=VARCHAR}, quantity=#{quantity,jdbcType=INTEGER},money=#{money,jdbcType=DOUBLE}
        WHERE number=#{number,jdbcType=VARCHAR}
    </update>

    <select id="searchFinanceByBianHao" resultType="com.macro.mall.dto.Finance">
          SELECT * FROM finance where number=#{number,jdbcType=VARCHAR}
    </select>

    <select id="getAll" resultType="com.macro.mall.dto.Finance">
        SELECT * FROM finance
    </select>
</mapper>